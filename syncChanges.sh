git config --global user.email "ludo@server.com"
git config --global user.name "Ludo Server"
git restore .
git pull origin
npm i
# npm version minor -f
# npm run release
# npm run build
# pm2 start processHandler
# git add .
# git commit -m "version bump"
# git push origin
